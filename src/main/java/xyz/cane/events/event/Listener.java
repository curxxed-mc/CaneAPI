package xyz.cane.events.event;

/**
 * A marker interface for a class that contains event handler methods.
 */
public interface Listener {
    // This is a "marker" interface, it doesn't need any methods.
}
